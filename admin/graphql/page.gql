query page($id: ID!) {
  page(id: $id) {
    id
    slug
    title {locale value}
    parent {id}
    layout
    published
    sections {
      key
      blocks {
        _type
        ref
      }
    }
  }
}