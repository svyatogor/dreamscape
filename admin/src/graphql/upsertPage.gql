mutation upsertPage($page: PageInput!, $locale: String) {
    upsertPage(page: $page, locale: $locale) {
      id
      slug
      title
      position
      parent {id}
      layout
      published
      sections
      properties
    }
  }